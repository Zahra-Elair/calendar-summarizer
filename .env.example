# Copy this file to `.env` and fill in your key.
# `.env` is gitignored, so your key never gets committed.

# Free Gemini API key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your_free_key_here

# Optional: override the model (defaults to gemini-2.0-flash)
# CALSUM_MODEL=gemini-2.0-flash
